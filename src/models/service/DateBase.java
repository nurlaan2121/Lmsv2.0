package models.service;

import models.Group;

import java.util.LinkedList;

public class DateBase {
    public LinkedList<Group> groups;

    public DateBase(LinkedList<Group> groups) {
        this.groups = groups;
    }
}
